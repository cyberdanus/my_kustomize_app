apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    name: web
  name: web
  namespace: ingress-nginx
spec:
  endpoints:
  - port: http
    path: /metrics
    interval: 5s
  selector:
    matchLabels:
      app: web-v1